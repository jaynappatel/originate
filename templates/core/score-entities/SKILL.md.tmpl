# /score-entities

Generalizes Topwater's `/score-companies`. Scores every discovered
{{vocabulary.entity}} against `originate.config.json`'s `criteria` block —
before a single dollar is spent on enrichment.

---
